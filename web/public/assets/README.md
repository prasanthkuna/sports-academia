# Static assets

Place generated images here. Full spec: `docs/landing-assets.md`

```
brand/      — product logo
landing/    — platform homepage (/)
academy/    — public academy page fallbacks
og/         — social preview images
testimonials/ — optional avatars
```

Paths are referenced from `src/lib/assets.ts`.
